@{
    ViewData["Title"] = "Admin Dashboard";
}

<div class="container">
    <h1>Admin Dashboard</h1>

    <div class="admin-links">
        <a href="@Url.Action("ProductMGMT", "Admin")" class="btn btn-primary">Manage Products</a>
        <a href="@Url.Action("Debug", "Admin")" class="btn btn-primary">View Debug Info</a>
    </div>
</div>